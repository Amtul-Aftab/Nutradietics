// Nutradietics Prisma schema
// Datasource + generator setup (task 0.2). Models are added in task 0.3.

generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "postgresql"
  url      = env("DATABASE_URL")
}
